// Prisma datasource + generator (SYSTEM_PROMPT §4, §7). Multi-file schema: model
// files live alongside this one in prisma/schema/ and are merged at generate time.
generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}
